#!/bin/sh
# Compatibility wrapper. The OpenWrt network implementation is shared by all
# Simo core providers and lives in /usr/libexec/simo/net/openwrt.sh.

exec /usr/libexec/simo/net/openwrt.sh singbox "$@"
